version: "3.8"
services:
  mongodb:
    image: mongo
    container_name: mongo_db
    volumes:
      - mongo-data:/data/db
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
  frontend:
    build: ./frontend
    container_name: frontend
    
  nginx:
    build: ./nginx
    container_name: nginx
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
volumes:
  mongo-data: